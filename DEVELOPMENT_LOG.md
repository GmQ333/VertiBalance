# VertiBalance 开发记录

本文件记录患者端完善过程中的功能、修复、设计决策和验证结果。

## 分支约定

- `dev`：日常集成分支。
- `feat/<name>`：功能开发分支，完成后合并到 `dev`。
- `fix/<name>`：缺陷修复分支，完成后合并到 `dev`。

Git 分支名不支持冒号，因此以 `/` 代替 `feat:xxxx` 和 `fix:xxxx` 中的冒号。

## 2026-08-27

### 开发基线

- 从 `main` 创建 `dev` 分支。
- 确认患者端优先实施范围：风险安全、问诊记录、结构化报告、挂号闭环、随访和针对性科普。
- 确认每个功能或修复在独立分支完成并验证后合并到 `dev`。

### 风险分诊安全修复（`fix/risk-triage`）

- 危险信号和高危病史规则增加中文否定语义识别，避免“没有说话不清”“无复视”等描述被误判。
- 管理员配置的自定义危险关键词同样应用否定语义识别。
- 移除患者前端重复维护的危险信号正则，以服务端风险结果作为唯一权威来源。
- 紧急提示出现后保留信息补充入口，同时隐藏普通快捷建议并持续提示立即就医。
- 患者报告严格区分紧急、高、中、低四级风险及对应就医时效。
- 增加否定描述、混合描述和自定义规则的单元测试。

### 患者问诊记录与结构化报告（`feat/patient-consultations`）

- 增加患者问诊轮次和报告的结构化 JSON 模型契约，并校验字段类型、风险等级、非确定性表述和采集字段。
- 规则风险与模型风险只取更高等级；危险信号始终提升为紧急，模型不能降低规则结论。
- 模型服务不可用或返回非法结构时生成明确标注的保守降级报告，并记录模型调用失败。
- 报告生成前要求普通问诊至少完成三轮症状采集；含危险信号的问诊可直接生成急诊报告。
- 患者端新增问诊记录中心，支持进行中问诊、完整历史对话、报告字段、既往史、用药、风险时效和继续问诊。
- 问诊页显示真实模型服务状态，避免在未配置密钥时展示“已连接”。
- 增加结构化问诊轮次、结构化报告和非法 JSON 的测试。

### 患者诊后闭环（`feat/patient-care-loop`）

- 挂号页要求明确选择要移交给医生的问诊报告，并按报告推荐科室优先排序号源。
- 随访反馈增加“过去 24 小时发作次数”，严重程度、频率或危险关键词触发异常关注。
- 已完成随访任务拒绝重复提交，避免覆盖既有医疗反馈。
- 科普页增加分类筛选和空结果回退，保留独立的健康教育免责声明。
- 更新患者 API 测试，覆盖频率持久化和重复反馈拦截。

### 跨平台验收（`feat/patient-e2e`）

- 修正 SQLite 文件权限测试：POSIX 环境继续校验 `0600/0700`，Windows 环境校验数据库文件和目录初始化结果，避免将 NTFS mode 位误判为业务失败。
- 全量 `npm test` 通过 63 项，`npm run build` 通过。
- 当前环境未安装浏览器自动化运行时，因此本轮以生产构建、HTTP 健康检查和 API 冒烟测试作为前端运行态验收；浏览器截图验收仍需在具备 Chromium/Playwright 的环境执行。

### 生产启动修复（`fix/production-start`）

- 使用 Node 生产入口设置 `NODE_ENV`，替换 Windows 无法执行的 Unix 内联环境变量语法。
- 生产静态文件服务显式导入 Express，确保 `npm start` 能正确加载构建产物。
- 已在 Windows 上完成生产模式验收：首页返回 200，健康接口返回 SQLite 就绪状态。

### 患者就医与科普引导（`feat/patient-guidance`）

- 智能问诊侧栏接入每轮真实分诊结果，展示推荐科室和就医时效，并可直接进入独立挂号页查看号源。
- 紧急风险只展示急救指引，不提供挂号跳转，避免在线流程延误急诊处理。
- 健康科普读取最近一份问诊报告，按风险等级与可能方向排序内容，并标识报告相关的推荐依据。
