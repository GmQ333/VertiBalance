# VertiBalance 开发记录

本文件记录患者端完善过程中的功能、修复、设计决策和验证结果。

## 分支约定

- `dev`：日常集成分支。
- `feat/<name>`：功能开发分支，完成后合并到 `dev`。
- `fix/<name>`：缺陷修复分支，完成后合并到 `dev`。

Git 分支名不支持冒号，因此以 `/` 代替 `feat:xxxx` 和 `fix:xxxx` 中的冒号。

## 2026-08-27

### 开发基线

- 从 `main` 创建 `dev` 分支。
- 确认患者端优先实施范围：风险安全、问诊记录、结构化报告、挂号闭环、随访和针对性科普。
- 确认每个功能或修复在独立分支完成并验证后合并到 `dev`。

### 风险分诊安全修复（`fix/risk-triage`）

- 危险信号和高危病史规则增加中文否定语义识别，避免“没有说话不清”“无复视”等描述被误判。
- 管理员配置的自定义危险关键词同样应用否定语义识别。
- 移除患者前端重复维护的危险信号正则，以服务端风险结果作为唯一权威来源。
- 紧急提示出现后保留信息补充入口，同时隐藏普通快捷建议并持续提示立即就医。
- 患者报告严格区分紧急、高、中、低四级风险及对应就医时效。
- 增加否定描述、混合描述和自定义规则的单元测试。

### 患者问诊记录与结构化报告（`feat/patient-consultations`）

- 增加患者问诊轮次和报告的结构化 JSON 模型契约，并校验字段类型、风险等级、非确定性表述和采集字段。
- 规则风险与模型风险只取更高等级；危险信号始终提升为紧急，模型不能降低规则结论。
- 模型服务不可用或返回非法结构时生成明确标注的保守降级报告，并记录模型调用失败。
- 报告生成前要求普通问诊至少完成三轮症状采集；含危险信号的问诊可直接生成急诊报告。
- 患者端新增问诊记录中心，支持进行中问诊、完整历史对话、报告字段、既往史、用药、风险时效和继续问诊。
- 问诊页显示真实模型服务状态，避免在未配置密钥时展示“已连接”。
- 增加结构化问诊轮次、结构化报告和非法 JSON 的测试。

### 患者诊后闭环（`feat/patient-care-loop`）

- 挂号页要求明确选择要移交给医生的问诊报告，并按报告推荐科室优先排序号源。
- 随访反馈增加“过去 24 小时发作次数”，严重程度、频率或危险关键词触发异常关注。
- 已完成随访任务拒绝重复提交，避免覆盖既有医疗反馈。
- 科普页增加分类筛选和空结果回退，保留独立的健康教育免责声明。
- 更新患者 API 测试，覆盖频率持久化和重复反馈拦截。

### 跨平台验收（`feat/patient-e2e`）

- 修正 SQLite 文件权限测试：POSIX 环境继续校验 `0600/0700`，Windows 环境校验数据库文件和目录初始化结果，避免将 NTFS mode 位误判为业务失败。
- 全量 `npm test` 通过 63 项，`npm run build` 通过。
- 当前环境未安装浏览器自动化运行时，因此本轮以生产构建、HTTP 健康检查和 API 冒烟测试作为前端运行态验收；浏览器截图验收仍需在具备 Chromium/Playwright 的环境执行。

### 生产启动修复（`fix/production-start`）

- 使用 Node 生产入口设置 `NODE_ENV`，替换 Windows 无法执行的 Unix 内联环境变量语法。
- 生产静态文件服务显式导入 Express，确保 `npm start` 能正确加载构建产物。
- 已在 Windows 上完成生产模式验收：首页返回 200，健康接口返回 SQLite 就绪状态。

### 患者就医与科普引导（`feat/patient-guidance`）

- 智能问诊侧栏接入每轮真实分诊结果，展示推荐科室和就医时效，并可直接进入独立挂号页查看号源。
- 紧急风险只展示急救指引，不提供挂号跳转，避免在线流程延误急诊处理。
- 健康科普读取最近一份问诊报告，按风险等级与可能方向排序内容，并标识报告相关的推荐依据。

### 患者界面结构拆分（`feat/patient-ui-structure`）

- 将智能问诊会话从根应用拆分到 `src/patient/Consultation.jsx`，隔离会话状态、危险提示、分诊卡和消息交互。
- 根应用保留角色路由与全局会话职责，患者问诊功能通过明确的报告回调连接到问诊记录页。

### 模型环境变量加载修复（`fix/model-env-loading`）

- 修复启动脚本只读取 `.env.local`、导致现有 `.env` 模型密钥未进入进程的问题。
- 开发、生产、备份和恢复命令统一先加载 `.env`，再由可选的 `.env.local` 覆盖本机差异配置。
- `.env` 与 `.env.local` 均保持在 Git 忽略规则内，模型密钥不会进入版本库或前端构建产物。

### 患者端浏览器验收（`feat/patient-browser-e2e`）

- 引入 Playwright 测试框架并复用本机 Microsoft Edge，不额外下载浏览器运行时。
- 浏览器测试使用独立端口、临时 SQLite 数据库和上传目录，不读写日常演示数据。
- 增加桌面端和移动端患者登录、五个核心服务页面导航、智能问诊状态和输入控件验收。
- 为移动导航、关闭导航和站内通知按钮补充无障碍名称，提升自动化定位及读屏体验。
- 浏览器验收发现并修复问诊记录空状态丢失页面标题和主操作的问题，空数据时仍保留清晰的页面上下文。

### 患者人工帮助闭环（`feat/patient-support`）

- 将智能问诊“联系”空按钮改为人工帮助表单，支持帮助类型、联系方式、问题摘要和最近处理状态展示。
- 新增患者帮助请求持久化表、患者范围查询、管理员列表与处理状态接口，并通过站内通知连接双方。
- 请求关联当前问诊；患者只能查看自己的请求，管理员处理后患者会收到状态通知，关键操作写入审计日志。
- 危险描述或紧急问诊会将请求标记为紧急，并明确提示立即急诊或呼叫 120，不能等待平台回电。

### 患者界面遗留清理（`feat/patient-ui-cleanup`）

- 删除不再路由的旧患者首页、静态报告和静态子页面，移除其中的假日期、假预约、假随访与无行为按钮。
- 患者未知路由统一回退到真实健康首页，不再进入历史演示组件。
- 移除只被旧页面引用的静态随访数据依赖，根应用继续收敛到真实 API 页面与角色路由。

## 2026-08-28

### 患者个人资料维护（`feat/patient-profile`）

- 患者可在账户菜单更新姓名、手机号、性别和年龄，保存后即时同步当前会话。
- 服务端校验姓名、手机号、年龄和手机号唯一性，账号标识不可由患者修改。
- 个人资料更新写入审计日志，接口返回脱敏后的公开用户信息。

### 个人资料更新修复（`fix/patient-profile`）

- 修复资料更新路由缺少认证中间件导致的“请求失败，请稍后重试”。
- 放大资料弹窗标题、字段标签、输入框和提交按钮字号，改善患者端可读性。

### 预约提醒与通知增强（`feat/patient-booking-reminders`）

- 通知中心支持全部标记已读，并根据预约、随访和人工帮助通知跳转到对应患者页面。
- 患者取消预约时同步通知对应医生，避免接诊队列信息滞后。
- 患者预约卡支持导出标准 ICS 日历文件，便于加入系统日历。

### 患者健康资料中心（`feat/patient-document-center`）

- 上传资料时可选关联本人问诊记录，便于医生按就诊上下文查看。
- 增加鉴权图片/PDF预览、原文件下载和二次确认删除操作。
- 资料预览和删除均执行患者范围权限校验并写入不可变审计记录。
- 重构文件列表操作区和移动端布局，明确展示文件类型、大小与上传时间。
